apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: algorythmos-ai-agents-ingress
  namespace: algorythmos-ai-agents
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: algorythmos-ai-agents.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: api-python-service
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 3000
  - host: api.algorythmos-ai-agents.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-python-service
            port:
              number: 8000
